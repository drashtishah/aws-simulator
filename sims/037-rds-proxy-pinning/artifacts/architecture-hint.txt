Finch Ledger invoice-processing architecture (Monday 14:30 UTC, incident state)

  SQS: invoice-creation-queue
         |
         | (event source mapping, 400 concurrent pollers)
         v
  Lambda: invoice-processor (x400 concurrent)
         |
         | (TCP :5432, proxy endpoint)
         v
  RDS Proxy: finch-proxy
         |
         | [PINNING ACTIVE - 400/450 connections pinned]
         v
  Aurora PostgreSQL 15: finch-invoicing-cluster (writer)
  db.t4g.medium | max_connections=450 | connections=450 (AT LIMIT)
         |
         +-- invoices table
         +-- invoice_number_seq  <-- pinning trigger
