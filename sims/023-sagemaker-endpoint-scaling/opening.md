6:14 AM Eastern. Arcline Logistics, on-call pager.

The arcline-route-optimizer endpoint is returning 503 ModelError responses. Eighty errors in the last sixteen minutes, climbing.

Lambda logs show arcline-route-handler retrying three times per request before giving up. The dispatch team is already on manual routing, twelve extra minutes per driver.

Your on-call lead, Priya, messages you: "Scaling should handle this. It never fired. I'm pulling the carriers on a call at 7 AM and I need something to tell them."

The endpoint is InService. The auto-scaling policy is configured. The morning rush window runs until 9 AM, 1,400 drivers waiting on routes.

Where do you start?
